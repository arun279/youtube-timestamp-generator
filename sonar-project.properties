# SonarCloud Configuration
# Free for open source projects at sonarcloud.io

# Project identification
sonar.projectKey=arun279_youtube-timestamp-generator
sonar.organization=arunkris
sonar.projectName=YouTube Timestamp Generator

# Source configuration
sonar.sources=web/src
sonar.exclusions=**/node_modules/**,**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx

# TypeScript configuration
sonar.typescript.tsconfigPath=web/tsconfig.json

# Disable coverage requirements (no tests yet)
sonar.coverage.exclusions=**/*

# Quality gate settings
# These can be adjusted in SonarCloud dashboard

# Language
sonar.language=ts

# Encoding
sonar.sourceEncoding=UTF-8


